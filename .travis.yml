language: node_js
node_js:
  - 13

install:
  - npm install
script:
  - npm build
  - npm publish
  - git add .
  - git stash
  - npm version patch -m "Bumping version %s [ci skip]"
  - git push --tags
